#include <string>
#include <vector>

class Balde {

    public:
        Balde(int tam);
        ~Balde();
        
        std::vector<std::string> info;
        int d_local;
        int quantidade;
};